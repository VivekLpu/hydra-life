// utils/sendSMS.js

/**
 * Temporary SMS sending placeholder.
 * This is disabled until SMS service is ready.
 * When enabling, restore the original API code here.
 */
const sendSMS = async (phone, message) => {
  console.log(
    `[SMS DISABLED] Would send to ${phone}: "${message}"`
  );
};

module.exports = sendSMS;
